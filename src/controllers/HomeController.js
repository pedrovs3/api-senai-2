class HomeController {
  // Lista (Get)
  async index(req, res) {
    res.status(200).json('a');
  }

  // Store (Post)

  // Update (Put or Patch)

  // Delete (Delete)
}

export default new HomeController();
